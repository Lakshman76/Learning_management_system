{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/build",
        "config": {
          "buildCommand": "npm run build"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ]
  }